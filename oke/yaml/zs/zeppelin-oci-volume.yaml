apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zeppelin-oci-pvc
spec:
  storageClassName: "oci-bv"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
